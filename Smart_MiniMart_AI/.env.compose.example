# Compose local — COPY thành `.env` rồi điền mật khẩu NGẪU NHIÊN (openssl rand -hex 24).
POSTGRES_USER=minimart
POSTGRES_PASSWORD=doi-toi-bang-mat-khau-ngau-nhien
POSTGRES_DB=smart_minimart
# pgAdmin (chỉ chạy khi dùng --profile tools)
PGADMIN_EMAIL=admin@minimart.local
PGADMIN_PASSWORD=doi-toi-bang-mat-khau-ngau-nhien
# Khóa chia sẻ backend ↔ OCR service (đồng thời đặt OCR_API_KEY trong backend/.env)
OCR_API_KEY=
